# Render Blueprint — backend API
# https://render.com/docs/blueprint-spec
services:
  - type: web
    name: datastride-sql-api
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: "20.19.0"
